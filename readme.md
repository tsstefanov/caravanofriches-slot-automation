How to run the tests

1. Install dependencies:
   npm install

2. Install Playwright browsers:
   npx playwright install

3. Run the tests:
   npx playwright test

Note: node_modules is not included, so make sure to run the steps above first.